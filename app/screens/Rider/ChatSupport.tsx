import SharedLayout from "@/components/Layout/SharedLayout";
import { View } from "react-native";

interface ChatSupportProps {}

const ChatSupport = ({}: ChatSupportProps) => {
  return (
    <SharedLayout>
      <View></View>
    </SharedLayout>
  );
};

export default ChatSupport;
